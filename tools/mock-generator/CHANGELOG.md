# Changelog

Este changelog inicia la nueva serie documental del modulo `tools/mock-generator`.

Los tags historicos del modulo siguen existiendo en Git. El ultimo tag observado en esta fase es `tools/mock-generator/v0.51.0`.

## [Unreleased]

### Added
- Documentacion fase 1 del modulo.
- Documentacion fase 2 de integracion ecosistemica del modulo.
- Estructura `docs/` con procesos, arquitectura e integracion.
- `Makefile` uniforme con validaciones y release-check.

### Changed
- El CLI ahora usa `--input` y apunta por defecto a `../../postgres/seeds/development`, una ruta existente en el repo.
